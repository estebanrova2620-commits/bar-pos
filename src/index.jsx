import React from "react";
import ReactDOM from "react-dom/client";
import BarApp from "./BarApp";
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<React.StrictMode><BarApp /></React.StrictMode>);

